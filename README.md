# BalanceCheck
This serves mostly as a tool to keep your monthly expenses, gains and loses in check. A budget feature can be used as well.
For now it starts as a simple script with a text-based interface. Eventually this might be an app, not sure.
